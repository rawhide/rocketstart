<section>
<%= form_for(@approval, :url => workflows_approvals_path(:approval_form_id => @report.id)) do |f| %>
  <div id='paper'>
  <div id='paper_inner'>
    <div align='center'><h2><%= @report.title %></h2></div>
    <hr />
    <br />
    <div align="center"><%= @report.header_text %></div>
    <br />
    <%= render :partial => 'fields', :locals => {:r => @report} %> 
    <div align="center"><%= @report.fotter_text %></div>
    <br />
  </div>
  </div>
  <br />
  <div align='center'>
    <div class="workflow-explain">
      <h3>この申請書は以下のフローで承認されます。</h3>
      <p><%= @report.workflow_templates.map{|wt| C.user_type.disp(wt.user_type) }.join(" > ") %></p>
    </div>
    <%= submit_tag("提出する", :confirm => 'この内容で送信してよろしいですか？') %>
  </div>
<% end %>
</section>
